# Auth
AUTH_SECRET=replace-with-a-long-random-string

# Stripe (required for real Apple Pay / Google Pay)
# Create keys at https://dashboard.stripe.com/apikeys
# Enable Apple Pay & Google Pay in Stripe Dashboard → Settings → Payment methods
# Add your domain under Apple Pay domains
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
